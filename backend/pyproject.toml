[project]
name = "backend"
version = "0.1.0"
description = "Backend Django do Portal VG 24H (PostgreSQL)"
requires-python = ">=3.12"
dependencies = [
    "cloudinary>=1.44.1",
    "django>=6.0.3",
    "pillow>=12.1.1",
    "psycopg[binary]>=3.3.3",
    "python-dotenv>=1.2.2",
]
